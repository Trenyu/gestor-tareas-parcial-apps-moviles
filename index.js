// Este archivo es el punto de entrada de la app.
// Expo lo usa para registrar el componente principal App.

import { registerRootComponent } from "expo";
import App from "./App";

registerRootComponent(App);
