// src/storage/storageKeys.js

// En este archivo guardamos las claves que vamos a usar en AsyncStorage.
// Sirve para no escribir strings repetidos en toda la app.

export const USER_KEY = "@usuario";
export const TASKS_KEY = "@tareas";
